# Frontend Setup

## Prerequisites

- Node.js >= 18

## Install and Run

```bash
cd frontend
npm install
npm run dev
```

## Environment Variables

Create a `.env.local` file:

```
NEXT_PUBLIC_API_URL=http://localhost:8080/api
```
